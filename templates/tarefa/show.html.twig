{% extends 'base.html.twig' %}

{% block title %}Tarefa{% endblock %}

{% block body %}
    <h1>Tarefa</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tarefa.id }}</td>
            </tr>
            <tr>
                <th>NomeDaTarefa</th>
                <td>{{ tarefa.nomeDaTarefa }}</td>
            </tr>
            <tr>
                <th>Custo</th>
                <td>{{ tarefa.custo }}</td>
            </tr>
            <tr>
                <th>DataLimite</th>
                <td>{{ tarefa.dataLimite ? tarefa.dataLimite|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>OrdemDaApresentacao</th>
                <td>{{ tarefa.ordemDaApresentacao }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tarefa_index') }}">back to list</a>

    <a href="{{ path('app_tarefa_edit', {'id': tarefa.id}) }}">edit</a>

    {{ include('tarefa/_delete_form.html.twig') }}
{% endblock %}
